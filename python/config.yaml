---

- psu:
    port: "/dev/ttyUSB0"

- epics:
    # $HOSTNAME: machine hostname
    prefix: "$hostname:PS:"
    
- http:
    enable: on
    url: "https://telegraf:8186/write"
    username: "myuser"
    password: "mypass"
